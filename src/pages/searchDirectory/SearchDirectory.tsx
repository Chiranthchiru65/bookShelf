import * as React from "react";

interface SearchDirectoryProps {}

const SearchDirectory: React.FunctionComponent<SearchDirectoryProps> = () => {
  return <>search directory</>;
};

export default SearchDirectory;
